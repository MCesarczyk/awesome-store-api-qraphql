extend type Query {
	category(id: ID!): Category
	categories(first: Int, skip: Int): [Category]
}

type Category {
	id: ID!
	name: String!

	createdAt: DateTime!
	updatedAt: DateTime!

	products: [Product]!
}
