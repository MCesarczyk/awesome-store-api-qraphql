extend type Query {
	order(id: ID!, status: String): Order
	orders(first: Int, skip: Int, status: String): [Order]
}

type Order {
	id: ID!
	total: Int!
	status: String!

	orderItems: [OrderItem!]!

	createdAt: DateTime!
	updatedAt: DateTime!
}
