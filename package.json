{
  "name": "cnc-tool-store-api-graphql",
  "version": "1.0.0",
  "engines": {
    "node": "20.x"
  },
  "description": "",
  "type": "module",
  "main": "index.js",
  "scripts": {
    "build": "tsc",
    "serve": "node dist/src/server.js",
    "deploy": "npm install && npm run prisma migrate dev && npm run prisma generate && npm run dev",
    "codegen": "graphql-codegen --config .graphqlrc.ts",
    "postinstall": "npm run codegen",
    "dev": "tsx watch src/server.ts",
    "seed": "tsx prisma/seed.ts",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@eddeee888/gcg-typescript-resolver-files": "^0.6.0",
    "@faker-js/faker": "^8.0.2",
    "@graphql-codegen/cli": "^5.0.0",
    "cz-conventional-changelog": "^3.3.0",
    "graphql": "^16.8.0",
    "prettier": "^3.0.3",
    "prisma": "^5.2.0",
    "tsx": "^3.12.8",
    "typescript": "^5.2.2"
  },
  "dependencies": {
    "@apollo/server": "^4.9.3",
    "@prisma/client": "^5.2.0",
    "graphql-scalars": "^1.22.2"
  },
  "config": {
    "commitizen": {
      "path": "./node_modules/cz-conventional-changelog"
    }
  }
}
